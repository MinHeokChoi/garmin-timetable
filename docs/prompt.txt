아래 시간표 사진을 읽고, 이 형식의 한 줄만 출력해줘.

월:09:00-09:50,근로,문헌관;10:00-11:50,데이터베이스 시스템,T0503|화:11:00-12:50,생물학,K502

- 요일은 파이프(|)로 구분하고, 수업이 없는 요일은 넣지 않는다.
- 한 수업은 [시작-종료,과목명,강의실] 이고 수업 사이는 세미콜론(;)으로 구분한다.
- 시간은 24시간제 HH:MM. 강의실을 모르면 생략하고 앞의 쉼표도 뺀다.
- 연강(2~3교시가 붙어 있는 것)은 하나로 합쳐 시작~끝 시간으로 적는다.
- 과목명은 시간표에 적힌 그대로 쓴다. 임의로 줄이지 않는다.
- 다만 8자가 넘는 이름은 시계에서 두 줄로 나뉜다. 자연스럽게 끊기는 자리에
  띄어쓰기를 하나 넣어준다. 두 줄 길이가 비슷해지는 자리를 고른다.
    데이터베이스시스템 -> 데이터베이스 시스템
    비디오이미지프로세싱 -> 비디오이미지 프로세싱
  8자 이하는 그대로 둔다. (운영체제, 생물학)
- 흐릿해서 안 읽히는 칸은 추측하지 말고 ??? 로 표시한다.
