# 디자인 시스템

Forerunner 255 시간표 앱의 화면 규칙. 구현은 [`source/Theme.mc`](source/Theme.mc)에 있고,
이 문서는 그 값을 왜 그렇게 정했는지를 적는다.

## 전제

- **화면**: 260×260 원형 MIP. fr255s 계열은 218×218.
- **MIP는 백라이트가 꺼진 상태가 기본이다.** 반사광으로 보기 때문에 대비가 곧 가독성이다.
- **터치가 없다.** 모든 탐색은 버튼.
- **기기 폰트만 쓸 수 있다.** 크기를 임의로 정하지 못하고, 정해진 몇 단계 중에서 고른다.
- **한글은 시계 언어가 한국어일 때만 렌더링된다.** 영어 설정에서는 `?`로 깨진다.

## 원칙

> 손목을 들어 2초 안에 읽힌다.

이 하나에서 나머지가 파생된다. 한 화면에 정보는 **최대 4줄**(상태 / 과목명 / 시간 / 강의실)이고,
그 이상은 넣지 않는다.

## 색

배경은 순수 검정이다. MIP에서 가장 잘 보이고 전력도 가장 적게 쓴다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `BG` | `0x000000` | 배경 |
| `TEXT_PRIMARY` | `0xFFFFFF` | 과목명, 시간 |
| `TEXT_SECONDARY` | `0xAAAAAA` | 강의실 — 한 단계 낮은 정보 |
| `ACCENT_NOW` | `0x00FF00` | 진행 중 |
| `ACCENT_NEXT` | `0x55AAFF` | 앞으로 올 일정 |
| `ACCENT_PREV` | `0xAAAAAA` | 지나간 일정 |
| `ACCENT_DONE` | `0xFFAA00` | 오늘 끝 |

**강조색은 상태 라벨 한 줄에만 쓴다.** 본문까지 색을 입히면 대비가 무너지고 훑어보기 어려워진다.

`ACCENT_NEXT`에 기본 파랑(`0x0000FF`)을 쓰지 않는 이유: MIP 반사광에서 거의 검정으로 보인다.
시뮬레이터에서 나란히 놓고 비교해 밝은 하늘색으로 바꿨다.

## 타입 스케일

기기 폰트는 크기를 못 정하므로, 역할마다 **폰트 사다리**를 두고 폭에 맞을 때까지 내려간다.

| 역할 | 사다리 |
|---|---|
| 과목명 (한 줄) | `LARGE` → `MEDIUM` |
| 과목명 (두 줄) | `MEDIUM` → `SMALL` → `TINY` |
| 시간 | `MEDIUM` → `SMALL` → `TINY` |
| 강의실 | `SMALL` → `TINY` |
| 상태 라벨 | `TINY` 고정 |

**긴 과목명은 폰트를 줄이기 전에 두 줄로 접는다.** `MEDIUM`에 한 줄로 안 들어가면
글자 수 절반에서 나눈다. 한글 과목명은 띄어쓰기가 없어서 단어 경계를 쓸 수 없다.

`비디오이미지프로세싱` → `비디오이미` / `지프로세싱` (2줄, MEDIUM)
`운영체제` → 한 줄, LARGE

작은 폰트 한 줄보다 큰 폰트 두 줄이 손목에서 더 잘 읽힌다.

## 레이아웃

### 원형 화면의 가용 폭

사각 화면이면 글자 최대 폭이 그냥 화면 폭이지만, **원형은 위아래로 갈수록 좁아진다.**
반지름 `r`인 원에서 중심으로부터 `dy`만큼 떨어진 가로선의 길이는 `2·√(r² − dy²)`다.

```
usableWidth(y) = 2·√(r² − dy²) − 2·EDGE_INSET
```

`EDGE_INSET`은 12px. 각 요소는 자기가 놓일 높이의 가용 폭을 기준으로 폰트를 고른다.
고정 비율(`화면폭 × 0.86`)로 근사하면 위아래 요소가 테두리를 넘거나, 반대로 중앙에서
쓸 수 있는 폭을 낭비한다.

### 세로 리듬

세로 위치는 화면 높이에 대한 %로 둔다. 260px과 218px에서 같은 비율로 앉는다.

| 값 | 위치 |
|---|---|
| `LABEL_Y_PCT` | 18% — 상태 라벨의 세로 중심 |
| `CONTENT_Y_PCT` | 56% — 본문 묶음의 광학 중심 |
| `GAP_TITLE` | 10px — 과목명 → 시간 |
| `GAP_TIME` | 6px — 시간 → 강의실 |

본문은 **고정 좌표에 하나씩 찍지 않는다.** 과목명 줄 수와 실제 폰트 높이를 재서 묶음 전체
높이를 구한 뒤, 그 묶음을 `CONTENT_Y_PCT`에 세로 중앙 정렬한다. 그래서 한 줄짜리
`운영체제`와 두 줄짜리 `AIML응용프로젝트1`이 둘 다 화면 가운데에 앉는다.

## 상태 체계

현재 시각을 기준으로 보고 있는 일정의 위치를 라벨 한 줄로 표시한다.

```
PREV -2   PREV -1   PREV   NOW   NEXT   NEXT +1   NEXT +2   DONE
└──────── 지나감 ────────┘   ↑    └──────── 남음 ────────┘
                          현재
```

- `NOW` — 지금 진행 중
- `NEXT` / `NEXT +n` — 다음 일정, 그 이후
- `PREV` / `PREV -n` — 직전 일정, 그 이전
- `DONE` — 오늘 일정 끝 / 오늘 일정 없음

앱을 열면 항상 `NOW`(없으면 `NEXT`)에서 시작한다. 오늘 일정이 다 끝났으면 `DONE`.

## 규칙

- **강의실이 비어 있으면 그 줄을 그리지 않는다.** 근로 블록이 여기 해당한다.
  빈 줄을 남기면 레이아웃이 아래로 쏠린다.
- **상태 라벨은 항상 그린다.** 위치가 고정이라 화면이 바뀌어도 눈이 흔들리지 않는다.
- **30초마다 다시 그린다.** 화면을 켜 둔 채 수업 경계를 넘어가도 `NOW`/`NEXT`가 어긋나지 않는다.
- **비ASCII 기호를 쓰지 않는다.** `→` `…` 같은 문자는 기기 폰트에 없어서 빈 네모로
  나온다. 한글은 시계 언어가 한국어면 나오지만 기호는 언어와 무관하게 없다.
  말줄임은 `..`, 화살표는 쓰지 않는다.

## 버튼

| 버튼 | 동작 |
|---|---|
| UP | 이전 일정 |
| DOWN | 다음 일정 |
| START | 현재 시각 위치로 복귀 |
| BACK | 앱 종료 |

`START`를 복귀로 둔 이유: 앞뒤로 몇 칸 넘긴 뒤 "지금 뭐지"로 돌아오는 게 가장 잦은 동작인데,
UP/DOWN을 반대로 같은 횟수만큼 누르게 하면 안 된다.

## 요약(Glance)

앱 본체와 같은 정보를 가로 띠에 담는다. 앱을 여는 것보다 빠르게 보는 게 목적이라
**탐색 기능이 없다.** 지금(또는 다음) 일정 하나만 보여준다.

```
NEXT                 ← 상태 (XTINY, 강조색)
근로                  ← 과목명 (SMALL → TINY → XTINY)
09:00-10:50  문헌관   ← 시간 + 강의실 (XTINY, TEXT_SECONDARY)
```

본체와 다른 점:

- **왼쪽 정렬.** 요약은 가로로 긴 띠라 가운데 정렬이 어색하다. 시스템이 왼쪽에
  앱 아이콘을 그리므로 텍스트는 그 오른쪽에서 시작한다.
- **폰트가 한 단계씩 작다.** 세로가 좁다 (`RAMP_GLANCE_TITLE`).
- **과목명을 두 줄로 접지 않는다.** 대신 폰트를 내린다. 세로 공간이 없다.
- **세 줄이 안 들어가면 상태 줄을 버린다.** 그 대신 과목명 자체를 상태 색으로
  칠해서 지금/다음을 구분한다.

**`dc.getWidth()` 를 그대로 믿으면 안 된다.** 시스템이 왼쪽에 앱 아이콘을 그리는데
`getWidth()` 는 그 영역까지 포함한 값을 돌려주고, 글자를 놓는 시작점(x=0)은 아이콘
오른쪽이다. 그대로 쓰면 오른쪽으로 넘쳐 잘린다. 218px 기기에서 실제로 잘렸다.

아이콘 폭을 알아낼 API 가 없어 실측으로 정했다 — 218px 는 68% 에서 잘리고 50% 에서
들어갔고, 260px 는 더 여유가 있다. **58%** 를 쓰고, 그래도 넘치면 `..` 를 붙여 자른다.

요약은 본체와 **다른 메모리 공간(64KB)**에서 돈다. 그래서 요약이 쓰는 코드에는
전부 `(:glance)` 표시가 붙어 있어야 한다 - `Schedule`, `Theme`, `GlanceView`,
그리고 `getGlanceView()`. 표시를 빠뜨리면 빌드는 되지만 요약이 안 뜬다.

**리소스도 스코프를 따로 지정해야 한다.** 코드에 `(:glance)` 를 붙여도 `Rez` 심볼은
따로다. 요약에서 `WatchUi.loadResource(Rez.Strings.X)` 를 쓰려면 그 문자열에
`scope="glance"` 가 있어야 하고, 없으면 **런타임에 앱이 죽는다**
(`Could not access symbol 'Rez'`). scope 는 하나만 지정할 수 있어서 본체와
요약이 같은 문자열을 공유할 수 없다 — 요약용 id 를 따로 둔다.

## 데이터 출처

시간표는 **앱 설정(Garmin Connect)** 에서 온다. 코드에 하드코딩하지 않는다.

처음에는 하드코딩이었다. 혼자 일주일 써 보는 게 목적이었고 그때는 그게 맞았다.
다른 사람에게 주는 순간 전제가 깨진다 — 강의실 하나 바뀔 때마다 그 사람 시계를
받아와서 다시 빌드해야 한다. **배포보다 수정이 자주 일어난다는 게 핵심이다.**

설정으로 빼면 `.prg` 가 모두에게 같아진다. 빌드는 한 번, 수정은 케이블 없이 폰에서.

입력값은 사람이 폰에서 직접 적거나 붙여넣는다. 그래서 파서는 관대해야 한다.
형식을 하나로 강요하지 않고, 하나 틀렸다고 전체가 죽지 않는다. 자세한 규칙은
[README](README.md) 참고.

## 코드 구조

```
Timetable.mc   설정 문자열 -> 블록 배열      (파싱·검증·정렬)
Schedule.mc    오늘 블록 + 현재 시각 대비 상태 (NOW/PREV/NEXT)
Theme.mc       색·타입 스케일·레이아웃 프리미티브
TimetableView  앱 본체 화면
GlanceView     요약 화면
```

경계를 이렇게 그은 이유:

**파싱과 시간 계산을 나눈다.** 파싱은 "사용자가 적은 글자"를 다루고, 시간 계산은
"지금 몇 시인가"를 다룬다. 바뀌는 이유가 다르다. 형식을 하나 더 받아주는 일과
상태 판정 규칙을 고치는 일이 서로를 건드리지 않아야 한다.

**Theme 은 뷰가 아니다.** 색과 폰트 사다리는 본체와 요약이 함께 쓴다. 뷰에 두면
한쪽만 고치는 실수가 난다.

**파싱은 그리기 경로에 두지 않는다.** `onUpdate` 는 30초마다, 그리고 버튼을 누를
때마다 호출된다. 거기서 설정 5개를 읽고 다시 파싱하면 낭비다. 뷰가 블록을 캐시하고,
설정이 바뀔 때(`onSettingsChanged`)와 화면을 열 때만 다시 읽는다.
**상태(NOW/NEXT)는 캐시하지 않는다** — 현재 시각으로 매번 다시 계산해야 화면이 낡지 않는다.

## 넓힐 때 지켜야 할 것

새 정보를 넣고 싶어지면 (남은 시간, 오늘 남은 수업 수, 내일 시간표 등)

1. **줄을 늘리기 전에 기존 줄을 대체할 수 있는지 본다.** 4줄이 상한이다.
2. **강조색을 새로 만들지 않는다.** 상태 라벨의 4색이 전부다.
3. **고정 좌표를 쓰지 않는다.** 측정 → 묶음 높이 계산 → 중앙 정렬 순서를 지킨다.
